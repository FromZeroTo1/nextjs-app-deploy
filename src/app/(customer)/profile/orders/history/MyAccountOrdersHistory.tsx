import { IFullUser } from '@/types/user.interface'
import { FC } from 'react'

const MyAccountOrdersHistory: FC<{ profile: IFullUser }> = ({ profile }) => {
	return <div>MyAccountOrdersHistory</div>
}

export default MyAccountOrdersHistory
